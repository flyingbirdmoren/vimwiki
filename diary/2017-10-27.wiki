= vim命令整理 =
- q:  q/  可进入搜索或者命令行列表，可进行一些编辑，回车后执行
- :ls bufferList
- <C-^> 切换到上一个buffer ,再按一次切换回来
- 在命令行中中按<C-r><C-w> 来输入光标处的文本
- 在命令行中按<C-r>寄存器的名字，可以输入寄存器的内容  beginend	
= 809运行起来 =
失败。。-》先搞合版.


= 改合版的bug =
- 气源地选择：
    - 列表，包括无坐标的
    - 下拉框，经纬度必须有值
